# Meta App (optional — do NOT commit real secret)
META_APP_ID=
# App secret is NOT used for WhatsApp webhooks — use System User token below
# META_APP_SECRET=

# Meta WhatsApp Cloud API
META_VERIFY_TOKEN=change_me_kvr_webhook_verify
META_ACCESS_TOKEN=
META_PHONE_NUMBER_ID=
META_WABA_ID=

# Doctor / business number (display only — configured in Meta)
CLINIC_PHONE=919491135557

# Alert destination — doctor receives booking/enquiry alerts (same or staff number)
DOCTOR_ALERT_PHONE=919491135557

PORT=8787
WEBHOOK_PATH=/webhook
